# chitori.fun 博客使用说明

项目位置：`/home/zoot/Desktop/blog`
线上地址：https://chitori-blog.vercel.app
仓库：https://github.com/chitorio/chitori-blog

发布流程：本地改 → `npm run dev` 预览 → `git add/commit/push` → Vercel 自动构建上线（约 40 秒）。

---

## 1. 写文章：`src/content/blog/`

两种放法：

- 单文件：`src/content/blog/my-post.md`
- 带图片的文件夹：`src/content/blog/my-post/index.md` + 同目录放 `thumbnail.jpg` 等资源

frontmatter 字段（定义在 `src/content.config.ts`）：

```yaml
---
title: '文章标题'            # 必填，最长 60 字
description: '一句话摘要'     # 必填，最长 160 字（会用于 SEO 和列表页）
publishDate: 2026-09-13 20:00:00   # 必填
updatedDate: 2026-09-14            # 选填
tags: [标签A, 标签B]               # 选填，会自动转小写去重
heroImage: { src: './thumbnail.jpg', alt: '封面图', color: '#B4C6DA' }  # 选填
language: '中文'                   # 选填，纯展示用
draft: true                       # 选填，true 只在本地 dev 显示，不发布
comment: false                    # 选填，false 关闭这篇的评论
---
```

生成模板（省得手写 frontmatter）：

```bash
npx astro-pure new -l zh -f "文章标题"   # -f 生成文件夹，-m 用 mdx，-d 建草稿
```

写正文用 Markdown，另外支持数学公式（KaTeX）、代码高亮带标题、MDX 组件（`.mdx` 里可以 `import { Aside } from 'astro-pure/user'`）。

## 2. 站点信息与开关：`src/site.config.ts`

| 想改什么 | 改哪里 |
| --- | --- |
| 站名 / 作者 / 简介 | `title`、`author`、`description` |
| 浏览器标签图标 | `favicon`（文件放 `public/favicon/`） |
| 分享卡片图 | `socialCard`（`public/images/social-card.png`） |
| 头像 | `logo.src`，默认 `src/assets/avatar.png` |
| 语言 / 日期格式 | `locale` 里的 `lang`、`dateLocale`、`dateOptions` |
| 顶部导航 | `header.menu`（增删 `{ title, link }`） |
| 页脚备案号、链接、社交图标 | `footer.links`、`footer.social`、`footer.credits` |
| 每页文章数 / 分享按钮 | `content.blogPageSize`、`content.share` |
| 评论系统 | `integ.waline`（`enable` 开关、`server` 服务端地址、`emoji`、`additionalConfigs`） |
| 站内搜索 | `integ.pagefind`（true/false） |
| 页脚随机语录 | `integ.quote.server` / `integ.quote.target` |
| 友链申请说明 | `integ.links.applyTip`、`integ.links.logbook` |
| 图片点击放大 | `integ.mediumZoom` |
| 自定义 CSS | `customCss` 数组 |

## 3. 页面文案：`src/pages/`

这些页面现在还是主题自带的示例文案（lorem ipsum），建议改成你自己的：

- `src/pages/index.astro`：首页。头像、技能标签 `languages` / `frontend` / `backend` 数组、项目卡片、最新文章列表都在这里
- `src/pages/about/index.astro`：关于页。个人介绍、爱好、工具箱、社交链接、Gossips
- `src/pages/projects/index.astro`：项目页（配图放 `src/assets/projects/`）
- `src/pages/links/index.astro` + `public/links.json`：友链页
- `src/pages/terms/`：隐私政策 / 条款页
- `src/assets/alipay-qrcode.jpg`、`src/assets/wechat-qrcode.jpg`：打赏二维码

## 4. 站点级设置

- `astro.config.ts`：`site`（正式域名，影响 canonical、RSS、sitemap）、`base`（部署到子路径时用）
- `src/content.config.ts`：文章字段的校验规则（想给文章加自定义字段就在这里加）
- `src/components/`：组件，比如 `src/components/waline/` 是评论区，`src/components/home/` 是首页模块

## 5. 本地命令

```bash
npm run dev      # 本地预览 http://localhost:4321 （热重载）
npm run check    # 类型检查
npm run build    # 完整构建（跟 Vercel 上跑的一样，构建不过 Vercel 也会失败）
```

## 6. 主题自带的文档

你站点的 `/docs` 页面就是主题官方文档，源码在 `src/content/docs/`：

- `setup/configuration.md`：所有配置项
- `setup/content.mdx`：怎么写文章
- `integrations/comment.mdx`：评论系统（Waline）
- `advanced/customize.mdx`：外观定制
- `advanced/update.mdx`：怎么升级主题

## 7. 评论系统（Waline）

自建服务端：https://chitori-waline.vercel.app （代码在 `/home/zoot/Desktop/waline-server`，数据库是 Vercel 上的 Neon）
后台管理：https://chitori-waline.vercel.app/ui
博客里指向服务端的配置在 `src/site.config.ts` 的 `integ.waline.server`。

## 8. 部署

- 博客：push 到 GitHub 的 `chitori-blog` 仓库 → Vercel 自动部署
- Waline 服务端：目前没接自动部署，改了代码后执行 `cd /home/zoot/Desktop/waline-server && npx vercel deploy --prod`
- 域名：`chitori.fun` 还没注册，买了以后在 Vercel 项目 → Settings → Domains 里填上，再按提示加 CNAME 解析
